name: Weekly WeChat Article

on:
  workflow_run:
    workflows:
      - Update Tour Data
    types:
      - completed
  workflow_dispatch:

jobs:
  generate-and-publish:
    if: github.event_name == 'workflow_dispatch' || github.event.workflow_run.conclusion == 'success'
    runs-on: ubuntu-latest
    steps:
      - name: Confirm fallback dispatch
        run: echo "Weekly WeChat Article fallback workflow triggered"
